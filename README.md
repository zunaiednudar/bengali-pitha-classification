# bengali-pitha-classification
Bengali pitha image classification using frozen ImageNet CNN features (Xception, EfficientNet, ResNet, ConvNeXt, MobileNet, DenseNet), PCA, and classical classifiers (SVM, Logistic Regression, Random Forest, XGBoost).
